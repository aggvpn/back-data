## BACKUP🎞️
```
wget -q -O /usr/bin/data "https://raw.githubusercontent.com/aggvpn/back-data/main/backup.sh" && chmod +x /usr/bin/data && apt install make && apt install gcc && apt install shc
```
## RESTORE🎞️
```
wget -q -O /usr/bin/backdata "https://raw.githubusercontent.com/aggvpn/back-data/main/restore.sh" && chmod +x /usr/bin/backdata && apt install make && apt install gcc && apt install shc
````
